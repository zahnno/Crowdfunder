== README

Crowdfunder is a web Application where users may post projects that require funding from others, while other users pledge to those projects to recieve awards.


In Terminal 

- Git clone SSH Key
- Rails s
- Open in webrowser localhost:3000

bundle install just to be safe